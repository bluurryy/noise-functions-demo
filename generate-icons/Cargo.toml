[package]
name = "generate-icons"
version = "0.1.0"
edition = "2021"

[dependencies]
image = { version = "0.25.10", default-features = false, features = [
  "ico",
  "png"
] }
noise-functions = "0.1.3"
